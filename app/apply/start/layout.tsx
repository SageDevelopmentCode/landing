export const metadata = {
  title: "Start Your Application — Sage Field",
  description:
    "Create an account or log in to begin your Sage Field application.",
};

export default function StartLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
