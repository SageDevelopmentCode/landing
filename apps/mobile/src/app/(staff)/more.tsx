import { View } from "react-native";

// Intentionally empty — the More tab press is intercepted in _layout.tsx
// to open the StaffMoreMenuSheet bottom sheet instead.
export default function StaffMoreScreen() {
  return <View style={{ flex: 1 }} />;
}
